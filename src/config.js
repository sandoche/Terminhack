const hacksMap = new Map([
  ['hack', 'hack.yml']
])

const config = {
  defaultTargetHost: 'www.sandoche.com',
  defaultHackId: 'hack',
  hacksMap,
  defaultHandler: 'block'
}

export default config
